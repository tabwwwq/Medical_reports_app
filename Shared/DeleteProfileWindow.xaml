<Window x:Class="MedicalReportsApp.DeleteProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Delete Profile"
        Height="900"
        Width="560"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FFF3F4F6">
    <Window.Resources>
        <Style x:Key="PrimaryDangerButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FFEF4444"/>
            <Setter Property="BorderBrush" Value="#FFDC2626"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFDC2626"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FF111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFD1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="56"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFF9FAFB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CodeTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Height" Value="64"/>
            <Setter Property="BorderBrush" Value="#FFD1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Border Background="White" BorderBrush="#FFFCA5A5" BorderThickness="1.5" CornerRadius="18" Padding="28">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid>
                    <TextBlock Text="Delete profile?" FontSize="24" FontWeight="Bold" Foreground="#FFDC2626" VerticalAlignment="Center"/>
                    <Button Content="✕" Width="34" Height="34" HorizontalAlignment="Right" Click="btnClose_Click" Style="{StaticResource SecondaryButtonStyle}" Padding="0" FontSize="16"/>
                </Grid>

                <Border Grid.Row="1" Margin="0,24,0,0" Background="#FFFEF2F2" BorderBrush="#FFFCA5A5" BorderThickness="1" CornerRadius="12" Padding="18">
                    <StackPanel>
                        <TextBlock Text="Warning! This action cannot be undone." FontSize="18" FontWeight="SemiBold" Foreground="#FFB91C1C"/>
                        <TextBlock Text="All user data will be removed from the database, including profile, visits, prescriptions, allergies, and related records." Margin="0,12,0,0" TextWrapping="Wrap" FontSize="16" Foreground="#FF991B1B"/>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Margin="0,18,0,0" Background="#FFF9FAFB" BorderBrush="#FFD1D5DB" BorderThickness="1" CornerRadius="12" Padding="16">
                    <StackPanel>
                        <TextBlock Text="Confirmation code will be sent to" FontSize="15" Foreground="#FF6B7280"/>
                        <TextBlock x:Name="txtEmailValue" Margin="0,10,0,0" FontSize="20" FontWeight="SemiBold" Foreground="#FF111827"/>
                    </StackPanel>
                </Border>

                <Border x:Name="successBorder" Grid.Row="3" Margin="0,18,0,0" Background="#FFF0FDF4" BorderBrush="#FF86EFAC" BorderThickness="1" CornerRadius="12" Padding="16" Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="Code sent to your email." FontSize="18" FontWeight="SemiBold" Foreground="#FF15803D"/>
                        <TextBlock x:Name="txtSuccessInfo" Margin="0,8,0,0" FontSize="15" Foreground="#FF166534" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <Border x:Name="dangerBorder" Grid.Row="4" Margin="0,18,0,0" Background="#FFFEF2F2" BorderBrush="#FFFECACA" BorderThickness="1" CornerRadius="12" Padding="16" Visibility="Collapsed">
                    <TextBlock Text="After entering the code, the profile will be permanently deleted from the database." TextWrapping="Wrap" FontSize="16" Foreground="#FFB91C1C"/>
                </Border>

                <StackPanel x:Name="codePanel" Grid.Row="5" Margin="0,22,0,0" Visibility="Collapsed">
                    <TextBlock Text="Enter confirmation code" FontSize="16" FontWeight="SemiBold" Foreground="#FF111827"/>
                    <TextBox x:Name="txtCode" Style="{StaticResource CodeTextBoxStyle}" Margin="0,12,0,0" MaxLength="6" TextAlignment="Center" PreviewTextInput="txtCode_PreviewTextInput"/>
                    <TextBlock Text="Enter the 6-digit code from the email" Margin="0,10,0,0" FontSize="14" Foreground="#FF6B7280" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="txtError" Margin="0,14,0,0" FontSize="14" Foreground="#FFDC2626" TextWrapping="Wrap" Visibility="Collapsed"/>
                </StackPanel>

                <StackPanel Grid.Row="6" Margin="0,24,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button x:Name="btnBack" Grid.Column="0" Content="Cancel" Click="btnBack_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                        <Button x:Name="btnAction" Grid.Column="2" Content="Send Code" Click="btnAction_Click" Style="{StaticResource PrimaryDangerButtonStyle}"/>
                    </Grid>
                    <Button x:Name="btnResend" Content="Send code again" Background="Transparent" BorderThickness="0" Foreground="#FF2563EB" FontSize="16" Margin="0,16,0,0" Click="btnResend_Click" Visibility="Collapsed" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
