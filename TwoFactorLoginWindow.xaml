<Window x:Class="MedicalReportsApp.TwoFactorLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Two-Factor Authentication"
        Height="420"
        Width="460"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FFF3F4F6">
    <Window.Resources>
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryRoundedButtonStyle" TargetType="Button" BasedOn="{StaticResource RoundedButtonStyle}">
            <Setter Property="Foreground" Value="#111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Border Background="White" BorderBrush="#FFD1D5DB" BorderThickness="1" CornerRadius="22" Padding="28">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Enter Google Authenticator code" FontSize="26" FontWeight="Bold" Foreground="#FF111827" TextWrapping="Wrap"/>
                <TextBlock Grid.Row="1" Margin="0,12,0,0" Text="Type the current 6-digit code from your authenticator app to continue." FontSize="15" Foreground="#FF4B5563" TextWrapping="Wrap"/>

                <StackPanel Grid.Row="2" Margin="0,22,0,0">
                    <Border Background="White" BorderBrush="#FFD1D5DB" BorderThickness="1" CornerRadius="14" Padding="10,0">
                        <TextBox x:Name="txtCode" Height="58" MaxLength="6" FontSize="28" FontWeight="SemiBold" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" BorderThickness="0" Background="Transparent"/>
                    </Border>
                    <TextBlock x:Name="txtError" Margin="0,12,0,0" Foreground="#FFDC2626" FontSize="14" Visibility="Collapsed" TextWrapping="Wrap"/>
                </StackPanel>

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,22,0,0">
                    <Button Content="Cancel" Width="120" Height="46" Margin="0,0,12,0" Style="{StaticResource SecondaryRoundedButtonStyle}" Click="btnCancel_Click"/>
                    <Button Content="Verify" Width="120" Height="46" Style="{StaticResource RoundedButtonStyle}" Click="btnVerify_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
