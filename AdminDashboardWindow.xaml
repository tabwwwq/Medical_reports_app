<Window x:Class="MedicalReportsApp.AdminDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Panel"
        Height="900"
        Width="1250"
        WindowStartupLocation="CenterScreen"
        Background="#FFF3F4F6">
    <Window.Resources>
        <Style x:Key="PageCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFF8FAFC"/>
            <Setter Property="BorderBrush" Value="#FFD1D5DB"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Padding" Value="24"/>
            <Setter Property="Margin" Value="0,0,0,18"/>
        </Style>

        <Style x:Key="ActionBlueButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="54"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FF2F6FED"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="44"/>
            <Setter Property="MinWidth" Value="96"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FF111827"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFD1D5DB"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="54"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFD1D5DB"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="16,0,16,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="12">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="14,0,14,0" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboBoxItemStyleRounded" TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="#FF2563EB"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="#FF2563EB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="#FF2563EB"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemBorder" Property="Background" Value="#FFF8FAFC"/>
                                <Setter Property="Foreground" Value="#FF2563EB"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#FF9CA3AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ComboBoxStyleRounded" TargetType="ComboBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="MinWidth" Value="160"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#FF2563EB"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFBFDBFE"/>
            <Setter Property="BorderThickness" Value="1.6"/>
            <Setter Property="Padding" Value="18,0,12,0"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItemStyleRounded}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="MainBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="18">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="44"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Margin="18,0,8,0"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}"/>
                                    <Border Grid.Column="1" Background="White" CornerRadius="0,18,18,0">
                                        <TextBlock Text="▼"
                                                   FontSize="11"
                                                   Foreground="#FF2563EB"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </Border>
                            <ToggleButton Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                                   Placement="Bottom"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Margin="0,6,0,0"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                        Background="White"
                                        BorderBrush="#FFD1D5DB"
                                        BorderThickness="1.2"
                                        CornerRadius="16"
                                        Padding="8">
                                    <ScrollViewer MaxHeight="260">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#FFBFDBFE"/>
                                <Setter TargetName="MainBorder" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="MainBorder" Property="BorderBrush" Value="#FFBFDBFE"/>
                                <Setter TargetName="MainBorder" Property="Background" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.65"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Style="{StaticResource PageCardStyle}" Padding="26,22,26,24">
            <StackPanel>
                <DockPanel Margin="0,0,0,18">
                    <StackPanel DockPanel.Dock="Left">
                        <TextBlock x:Name="txtAdminTitle" Text="Admin Panel" FontSize="30" FontWeight="Bold" Foreground="#FF111827"/>
                        <TextBlock x:Name="txtAdminInfo" Text="System logs" FontSize="15" Foreground="#FF6B7280" Margin="0,6,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="Refresh" Style="{StaticResource GhostButtonStyle}" Margin="0,0,10,0" Click="btnRefresh_Click"/>
                        <Button Content="Logout" Style="{StaticResource GhostButtonStyle}" Click="btnLogout_Click"/>
                    </StackPanel>
                </DockPanel>

                <TextBlock Text="System Logs" FontSize="24" FontWeight="Bold" Foreground="#FF111827" Margin="0,0,0,16"/>

                <Grid Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="170"/>
                        <ColumnDefinition Width="172"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtSearch" Grid.Column="0" Style="{StaticResource SearchBoxStyle}" VerticalContentAlignment="Center" KeyDown="txtSearch_KeyDown"/>
                    <ComboBox x:Name="cmbLogCategory" Grid.Column="1" Margin="12,2,162,2" Style="{StaticResource ComboBoxStyleRounded}" SelectionChanged="cmbLogCategory_SelectionChanged" Grid.ColumnSpan="2">
                        <ComboBoxItem Content="All" IsSelected="True"/>ы
                        <ComboBoxItem Content="Allergies"/>
                        <ComboBoxItem Content="Prescriptions"/>
                        <ComboBoxItem Content="Visit"/>
                        <ComboBoxItem Content="Password"/>
                        <ComboBoxItem Content="2FA"/>
                        <ComboBoxItem Content="Email"/>
                        <ComboBoxItem Content="Delete Account"/>
                        <ComboBoxItem Content="Profile"/>
                        <ComboBoxItem Content="Chronic Problems"/>
                        <ComboBoxItem Content="Security"/>
                        <ComboBoxItem Content="Other"/>
                    </ComboBox>
                    <Button x:Name="btnSearch" Grid.Column="2" Content="Search" Style="{StaticResource ActionBlueButtonStyle}" Margin="15,0,-3,0" Click="btnSearch_Click"/>
                </Grid>

                <TextBlock x:Name="txtSearchInfo" FontSize="13" Foreground="#FF6B7280"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource PageCardStyle}" Padding="18">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="logsPanel"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
